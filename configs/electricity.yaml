# Electricity dataset configuration
dataset:
  name: electricity
  prediction_length: 24
  lookback_window: 96
  dimension: 370

vae:
  latent_dim: 64
  embed_dim: 256

diffusion:
  diffusion_steps: 100
  embed_dim: 256
  guidance_strength: 5.0   # 论文: deterministic 数据集用更大 w
